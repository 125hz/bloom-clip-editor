{
  "name": "bloom-clip-editor",
  "version": "2.5.0",
  "description": "A simple clip editor to cut and merge videos quickly",
  "author": "125hz",
  "license": "ISC",
  "scripts": {
    "prepare-ffmpeg": "node scripts/prepare-ffmpeg.mjs",
    "prepare-vapoursynth": "node scripts/prepare-vapoursynth.mjs",
    "prepare-runtime": "npm run prepare-ffmpeg && npm run prepare-vapoursynth",
    "dev": "npm run prepare-runtime && tauri dev",
    "build": "npm run prepare-runtime && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
